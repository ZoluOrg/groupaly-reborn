import { Button } from "@/components/Button";

export default function Home() {
  return (
    <div className="h-screen w-screen items-center justify-center flex">
      <Button variant="accent">Delete Project</Button>
    </div>
  );
}
